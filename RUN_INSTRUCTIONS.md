# 如何运行实验

## ⚠️ 重要说明

**我创建的notebooks只包含代码框架，没有运行结果！** 您需要运行它们才能得到实际的模型性能数据。

---

## 🚀 快速开始（3步）

### 步骤 1: 创建并激活环境

#### 选项 A: 使用 Conda (推荐)

```bash
# 在项目目录下运行
cd /home/lyl610/DATA-144-final-project-flight-delay

# 创建环境
conda env create -f environment.yml

# 激活环境
conda activate flight-delay-env
```

#### 选项 B: 使用 pip + venv

```bash
# 创建虚拟环境
python -m venv flight-delay-venv

# 激活环境 (Linux/Mac)
source flight-delay-venv/bin/activate

# 激活环境 (Windows)
# flight-delay-venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

---

### 步骤 2: 启动 Jupyter Notebook

```bash
# 确保环境已激活，然后运行：
jupyter notebook
```

浏览器会自动打开，您会看到项目文件列表。

---

### 步骤 3: 运行 Notebooks

#### 选项 A: 快速测试（只运行主notebook，10-15分钟）

1. 打开 `flight_delay.ipynb`
2. 点击菜单 **Cell → Run All**
3. 等待所有单元格执行完毕
4. 查看最后的性能对比表格

**您会得到**:
- 基准模型 MSE
- 优化的 XGBoost MSE  
- LightGBM MSE
- CatBoost MSE
- 集成模型 MSE
- 最终性能对比图表

---

#### 选项 B: 完整实验（运行两个notebooks，2-3小时）

**第一步: 运行实验日志**
1. 打开 `experiment_log.ipynb`
2. 点击菜单 **Cell → Run All**
3. **这会花费 2-3 小时**（测试 20+ 个模型配置）
4. 结束后会生成：
   - `experiment_results.csv` - 所有实验详情
   - `model_comparison_summary.csv` - 模型排名

**第二步: 运行主notebook**
1. 打开 `flight_delay.ipynb`
2. 点击菜单 **Cell → Run All**
3. 查看最终结果

---

## 📊 您会得到什么结果

运行完成后，您会看到：

### 1. 主notebook输出示例：

```
======================================================================
                    FINAL MODEL COMPARISON
======================================================================
Model                      Mean_MSE  Improvement_vs_Baseline  Pct_Improvement
Baseline (Prior)           4677.00   0.00                     0.0%
Original XGBoost           3684.00   993.00                   21.2%
Optimized XGBoost          3520.00   1157.00                  24.7%
LightGBM                   3485.00   1192.00                  25.5%
CatBoost                   3510.00   1167.00                  25.0%
Ensemble (Weighted)        3460.00   1217.00                  26.0%
======================================================================

🏆 BEST MODEL: Ensemble (Weighted)
   MSE: 3460.00
   Improvement: 1217.00 (26.0%)
```

### 2. 实验日志输出示例：

```
======================================================================
FINAL EXPERIMENT SUMMARY
======================================================================
Experiment_ID  Model                        Mean_MSE  Std_MSE
F2             Ensemble (Weighted)          3460.23   45.12
E2_3           LightGBM Tuned 3            3485.67   48.33
E3_3           CatBoost Tuned 3            3492.45   47.89
...
======================================================================
```

---

## 🐛 可能遇到的问题

### 问题 1: Kaggle 数据集下载失败

**错误**: `kagglehub` 需要认证

**解决**:
```bash
# 需要 Kaggle API token
# 1. 去 https://www.kaggle.com/settings
# 2. 创建 API token
# 3. 下载 kaggle.json
# 4. 放到 ~/.kaggle/kaggle.json
```

或者手动下载数据集：
1. 访问: https://www.kaggle.com/datasets/shubhamsingh42/flight-delay-dataset-2018-2024
2. 下载 `flight_data_2018_2024.csv`
3. 修改notebook中的路径指向本地文件

---

### 问题 2: 内存不足

**错误**: `MemoryError` 或 `Killed`

**解决**: 
- 减少数据量（在数据加载后添加 `df = df.sample(n=100000, random_state=42)`）
- 减少 `n_estimators`（300 → 100）
- 减少 CV folds（3 → 2）

---

### 问题 3: 运行时间太长

**解决**:
- 只运行 `flight_delay.ipynb`（跳过 `experiment_log.ipynb`）
- 或者减少 `experiment_log.ipynb` 中的超参数配置数量

---

## 📝 运行后的下一步

1. **查看结果**: 看哪个模型MSE最低
2. **准备演讲**: 使用 `PRESENTATION_GUIDE.md` 中的结构
3. **记录数字**: 把实际的MSE数字填入演讲稿
4. **创建图表**: notebooks会自动生成可视化图表

---

## 💡 提示

- **第一次运行**: 建议只运行 `flight_delay.ipynb` 看看效果
- **如果满意**: 再运行完整的 `experiment_log.ipynb` 获取详细实验数据
- **演讲准备**: 主notebook的输出已经足够用于presentation

---

## 🆘 需要帮助？

如果遇到问题：
1. 检查环境是否正确激活
2. 确保所有依赖都已安装：`pip list | grep -E "xgboost|lightgbm|catboost"`
3. 查看错误信息的具体内容

---

**祝您实验顺利！** 🎉


